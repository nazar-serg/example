"use strict";

module.exports = function (args) {
  var id,
      i,
      length = args.length;
  if (!length) return "\x02";
  id = String(args[i = 0]);

  while (--length) {
    id += "\x01" + args[++i];
  }

  return id;
};